/**
 * Este paquete contiene las clases que implementan el patrón de diseño DAO (Data Access Object).
 * Cada clase está asociada a un rol específico (administrador, usuario, alumno, asignatura, matrícula, profesor, tutor)
 * y permite realizar las operaciones que corresponden a dicho rol en el contexto de la plataforma.
 * Ejemplo de operaciones incluyen asignación de asignaturas a profesores y matriculación de alumnos en asignaturas.
 */
package org.example.DAO;
